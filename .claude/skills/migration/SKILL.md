---
description: Create or modify database migrations
---

# Migrations

1. Place in `database/migrations/` — publishable via ServiceProvider
2. Key columns: `revolut_customer_id` on users table, `revolut_subscriptions` table