{
  "name": "laravel-cashier-revolut-e2e",
  "private": true,
  "description": "Local-only Playwright e2e for the Revolut Checkout Widget browser flow. NOT run in CI.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install-browser": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
